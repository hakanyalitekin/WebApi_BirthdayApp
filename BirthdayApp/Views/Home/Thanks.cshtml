@model BirthdayApp.Models.DavetiyeModel
@{
    ViewBag.Title = "Teşekkür Ederim!";
}

<h1> Teşekkürler, @Model.Ad! </h1>

<div class="lead">
    @if (Model.KatilmaDurumu == true)
    {
        @:Gelemene çok sevindim :) Bütün hazırlıklar tamamlandı!
        @Html.Action("Katilanlar","Home")
    }
    else
    {
        @:Gelemeyecek olmana üzüldüm! :(
    }
</div>
